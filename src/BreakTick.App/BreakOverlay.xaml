<Window x:Class="BreakTick.App.BreakOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="340" Height="310" WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent" Topmost="True">
    <Border Background="#FDFEFD" BorderBrush="#DDE7E1" BorderThickness="1" CornerRadius="18" Padding="28">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="✦" FontSize="30" Foreground="{StaticResource AccentBrush}" TextAlignment="Center" />
            <TextBlock x:Name="TitleText" FontSize="21" FontWeight="SemiBold" TextAlignment="Center" Margin="0,14,0,0" />
            <TextBlock x:Name="BodyText" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" TextAlignment="Center" Margin="0,10,0,0" />
            <TextBlock x:Name="TimeText" FontSize="34" FontWeight="SemiBold" TextAlignment="Center" Margin="0,18,0,0" />
            <TextBlock x:Name="WarningText" Foreground="#B76B23" TextAlignment="Center" Margin="0,8,0,0" />
            <Button x:Name="ActionButton" Click="Action_Click" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" Margin="0,20,0,0" />
        </StackPanel>
    </Border>
</Window>
